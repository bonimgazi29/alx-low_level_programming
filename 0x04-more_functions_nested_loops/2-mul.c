#include "main.h"

/**
* mul - Multiplies two integers
* @x: The first number to multiply
* @x: The second number to multiplication between x param and y param
*/

int mul(int x, int y)
{
	return (x * y);
}
