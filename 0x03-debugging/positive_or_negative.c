#include "main.h"
/**
 * positive_or_nagetive - checks for positive ornegative numbers
 *
 * 0i: the number to be checked
 *
 * Return: always 0
 */

void positive_or_negative(int i)
{
	if (i < 0)
		printf("%d is negetive", i);
	else
		printf("%d positive", i);
}
